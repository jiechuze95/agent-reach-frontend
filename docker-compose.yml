version: '3.8'
services:
  app:
    build: .
    ports:
      - "8001:8001"
    environment:
      - HOST=0.0.0.0
      - PORT=8001
      - CORS_ORIGINS=http://localhost:5173,http://localhost:3000
      - DEV_MODE=true
    restart: unless-stopped
