version: '3.8'
services:
  app:
    build: .
    ports:
      - "8001:8001"
    environment:
      - PORT=8001
      - CORS_ORIGINS=http://localhost:5173,http://localhost:3000
    restart: unless-stopped
